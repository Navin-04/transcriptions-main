/** @type {import('next').NextConfig} */
const nextConfig = {
  // Simple configuration without experimental features
  // App directory is stable in Next.js 14, no need for experimental flags
}

module.exports = nextConfig
